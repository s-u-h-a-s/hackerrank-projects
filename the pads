select concat(name,"(",left(occupation,1),")") from occupations order by name;
select "There are a total of ",count(name),concat(lcase(occupation),"s.") from occupations group by occupation order by count(name),occupation;
